<%= form_for(@producto) do |f| %>
	<% @producto.errors.full_messages.each do |message| %>
		<div class="be-red white top-space">
			* <%= message %>
		</div>
	<%end%>
	<div class="field">
		<%= f.text_field :codigo, placeholder: "Codigo del producto", class:"form-control"%>
	</div class="field">
	<div>
		<%= f.text_area :nombre, placeholder: "Escribir el nombre del producto", style: "height: 250px;", class:"form-control"%>
	</div>
	<div class="field">
		<%= f.submit "Guardar", class:"btn be-red white"%>
		</div>
<%end%>	